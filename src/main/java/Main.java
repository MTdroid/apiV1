/*
import org.hibernate.Session;
import ru.kata.entity.adminUserRestController.UserTable;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.List;

import static ru.kata.environments.UserDatabaseConfiguration.getSession;

public class Main {
    public static void main(String[] args) {

        String url = "jdbc:mysql://94.198.50.185:3309/jm_ewp?useUnicode=true&serverTimezone=UTC&useSSL=true&verifyServerCertificate=false";
        String username = "root";
        String password = "mysql12345";

        try (Connection connection = DriverManager.getConnection(url, username, password)) {
            System.out.println("Успешное подключение к базе данных MySQL!");
        } catch (SQLException e) {
            System.out.println("Ошибка подключения к базе данных MySQL!");
            e.printStackTrace();
        }


    }
}

*/
